# apps.py placeholder
from django.apps import AppConfig

class TaAssignmentsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ta_assignments'
    verbose_name = 'TA Assignments' # Optional: for a more human-readable name in admin